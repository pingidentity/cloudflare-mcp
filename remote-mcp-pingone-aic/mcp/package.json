{
	"name": "remote-mcp-ping-aic",
	"description": "MCP server secured with PingOne AIC",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"clean": "rm -rf dist .wrangler node_modules tsconfig.tsbuildinfo worker-configuration.d.ts || true",
		"build": "wrangler types && tsc --build",
		"dev": "wrangler dev",
		"deploy": "npm run build && wrangler deploy"
	},
	"dependencies": {
		"@modelcontextprotocol/sdk": "1.26.0",
		"agents": "^0.2.23",
		"hono": "^4.10.6",
		"jose": "^6.1.2",
		"zod": "^3.23.3"
	},
	"devDependencies": {
		"@types/node": "^24.10.1",
		"typescript": "5.9.3",
		"wrangler": "^4.50.0"
	}
}
